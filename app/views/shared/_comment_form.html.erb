<%= form_for([post, post.comments.build]) do |f| %>
  <div class="field">
    <%= f.text_area :content, placeholder: 'Write a comment...' %>
  </div>
  
  <div class="action">
    <%= f.submit 'Comment' %>
  </div>
<% end %>